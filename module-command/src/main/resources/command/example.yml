
name: 'exam'

main:
  execute: |-
    print "Hello, World!"

components:
  script:
    parent: 'main'

  run:
    parent: 'script'
    parameters:
      - name: 'scriptId'
    execute: |-
      tell hi!
      tell &scriptId
